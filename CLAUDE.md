# pascal-amqp-faa — contexto do projeto

Cliente AMQP 0-9-1 dual-compiler (**FPC/Lazarus + Delphi**) numa única codebase, MIT. É o porte multiplataforma da `../delphi-amqp-faa` (mesmo autor); aquela permanece como projeto à parte, Delphi/Windows-only. A arquitetura, os invariantes de concorrência e as lições de protocolo estão documentados no `CLAUDE.md` de lá e valem aqui integralmente.

## Regra inegociável: proveniência do código (herdada da lib original)

**Nunca copiar, adaptar ou se basear em código-fonte de bibliotecas Pascal/Delphi AMQP de terceiros sem licença permissiva clara.** Este projeto deriva exclusivamente da `delphi-amqp-faa` (MIT, mesmo autor) e da especificação AMQP 0-9-1. Consultas a outras implementações são permitidas apenas como conhecimento de protocolo, nunca como fonte a transcrever.

## Regras da codebase dual (Delphi + FPC 3.2.2)

Toda unit começa com `{$I amqp.inc}` (ativa `{$MODE DELPHI}` no FPC e define `AMQP_WINDOWS`). O que NÃO usar, e o que usar no lugar:

| Proibido (não existe no FPC 3.2) | Usar |
|---|---|
| `reference to procedure` / métodos anônimos / `TThread.CreateAnonymousThread` | `procedure ... of object`; work items (`TAMQPWorkItem`) para capturar estado; threads nomeadas |
| `System.Threading` (`TTask.Run`) | `AmqpPool.Queue(...)` de `AMQP.Threading` |
| `System.TMonitor` (Enter/Wait/PulseAll) | `TAMQPMonitor` de `AMQP.Threading` |
| `TInterlocked.*` / `AtomicXxx` direto | `AmqpAtomicInc/Dec/Get/Read64/Write64` de `AMQP.Threading` |
| `TThread.GetTickCount64` | `AmqpTickMs` |
| `System.Net.Socket` direto | `TAMQPTcpSocket` de `AMQP.Transport` |
| `TEncoding.UTF8.GetBytes/GetString` | `AmqpUtf8Encode/Decode` de `AMQP.Wire` (no FPC respeitam o codepage dinâmico) |
| uses com namespace (`System.SysUtils`) | nome curto (`SysUtils`); no Delphi resolve via unit scope names `System;Winapi` |
| inline `var` em bloco | declarar no `var` da rotina |
| `TStringHelper` (`.Split` etc.) | rotinas manuais (`Pos`/`Copy`) |
| `TArray.Sort<T>` | ordenação manual (não existe no Generics.Collections do FPC) |

Gotchas de FPC já encontrados no porte:

- `TValue.From<TArray<TValue>>(x)` não parseia (o `>>` vira `shr` e nem com espaço passa) → usar o alias `TAMQPValueArray` (AMQP.Wire). `TValue.AsType<T>` não existe → `GetArrayLength/GetArrayElement`.
- `Boolean` tem `Kind = tkBool` no FPC (não `tkEnumeration`); `AnsiString` é `tkAString` — os `case AValue.Kind` do codec tratam os dois via `{$IFDEF FPC}`.
- Enum anônimo inline como campo de classe não compila → tipo nomeado (`TAMQPAsmState`).
- `PWideChar(string)` não existe (string é Ansi) → campos que vão para APIs wide são `UnicodeString` (ver `AMQP.Transport.Tls.FTargetName`).
- `Format('%x')` com `LongInt` negativo imprime 16 dígitos → passar `Cardinal(valor)`.
- Fontes em UTF-8 **com BOM**: Delphi exige o BOM para ler UTF-8; o FPC com BOM trata literais como UTF-8 corretamente. Manter o BOM ao criar units novas.
- FPC 3.2.2 trava com **erro interno** (`Internal error 2015071704` / `200510032`) em duas combinações específicas envolvendo `TValue`: (1) encadear `.Put(...)` de `TAMQPFieldTable` terminando num `TValue.From<T>(literal)` inline — separar em chamadas `.Put()` distintas resolve; (2) `Tabela['chave'].AsString` / `.AsExtended` / `.AsObject` encadeado direto no indexador — atribuir o resultado do indexador a uma variável `TValue` local antes de chamar o accessor resolve (`.AsBoolean`/`.AsInteger`/`.AsInt64` encadeados direto não têm esse problema). Achado portando `tests\Unit\fpc\AMQP.WireTests.pas`.
- Apps console FPC puro (fora do Lazarus/LCL) têm `DefaultSystemCodePage` diferente de UTF-8 por padrão — strings acentuadas literais (mesmo com o `.pas` em UTF-8 com BOM) saem transcodificadas errado ao passar por `AmqpUtf8Encode`. Chamar `SetMultiByteConversionCodePage(CP_UTF8)` no início do `program` resolve (ver `tests\Unit\fpc\AMQPUnitTestsFpc.lpr`).
- `TInterlocked` (System.SyncObjs) não existe no FPC → usar `AmqpAtomicInc/Dec/Get/Set/CompareExchange` de `AMQP.Threading` (`AmqpAtomicSet`/`AmqpAtomicCompareExchange` foram adicionados especificamente para portar os testes de integração, que usavam `TInterlocked.Exchange`/`.CompareExchange(x,new,old)`).
- `TTestCase.AssertException` do FPCUnit exige a classe **exata** da exceção (`E.ClassName = AExceptionClass.ClassName`) — não aceita subclasse, diferente do `Assert.WillRaise(proc, EAlgumaClasse)` do DUnitX (que aceita `is`/herança) e do `Assert.WillRaise(proc)` sem classe (qualquer exceção). Para "qualquer exceção", usar `try/except` manual com uma flag booleana + `AssertTrue` (não `Fail()` dentro do próprio `try`, que seria capturado pelo `except`, já que `EAssertionFailedError` também é `Exception`).
- `System.Threading` (`TParallel.For`) não existe no FPC — para testes que precisavam de N chamadas concorrentes, usar `AmqpPool.Queue(TAMQPWorkItem)` da própria lib. Cuidado ao compartilhar arrays entre o item de trabalho e o chamador: um parâmetro de construtor tipado como `array of T` é *open array* (semântica de valor, copia os elementos) — usar um tipo nomeado de dynamic array (`TFooArr = array of T`) como parâmetro para de fato compartilhar o mesmo buffer.
- Citar um diretivo tipo `{$IFDEF FPC}` dentro de um comentário de prosa `{ ... }` quebra a compilação: o `}` do próprio texto citado fecha o comentário mais cedo, e o resto vira código inválido. Escrever "condicional" ou similar em vez do `{$...}` literal ao documentar isso em comentários.
- Amostras VCL/LCL (samples `*Vcl`): `TThread.Queue`/`.Synchronize` do FPC só têm o overload `TThreadMethod = procedure of object` (sem parâmetros) — sem o overload de closure anônima do Delphi. Toda chamada `TThread.Queue(nil, procedure ... end)` (usada pra levar resultado de callback da lib, que roda numa thread do pool, de volta pra UI) precisa virar um método nomeado. Como várias threads do pool chamam isso concorrentemente, um campo compartilhado na form teria corrida — em vez disso, cada chamada cria um pequeno objeto de "marshal" descartável (dados da chamada + `procedure Execute; begin Form.Metodo(...); Free; end;`) e enfileira `TThread.Queue(nil, LMarshal.Execute)`. Ver `samples\RetaguardaVcl\uRetaguardaMain.pas` (`TRecebidaMarshal`/`TStatusMarshal`).
- `System.TMonitor` (usado nos samples VCL originais pra proteger `FPending`) não existe no FPC → `TCriticalSection` (`SyncObjs`), mesmo padrão já usado no resto do projeto.
- **`TValue.GetArrayElement` sobre `TArray<TValue>` devolve o elemento re-embrulhado no FPC**: um `TValue` de `Kind=tkRecord` contendo o `TValue` interno — `IsObject`/`IsArray`/`As*` falham no embrulho (o `TValue.Make` do Delphi colapsa TValue-em-TValue; o do FPC 3.2 não). Mordeu ao ler as entradas do header `x-death` (array de tabelas) no sample RetryDlqVcl — e o próprio destrutor da `TAMQPFieldTable` vazava as tabelas aninhadas em arrays no FPC por isso. Correção: `AmqpUnwrapValue` (AMQP.Wire), idempotente e no-op no Delphi — usar após **todo** `GetArrayElement` de um array de field-values. Coberto por `Decode_ArrayDeTabelas_EstiloXDeath` nas duas suítes unitárias.
- **FPC descarta `TThread.Queue(nil, ...)` postado por uma thread que morre antes de a main thread bombear a fila**: `TThread.Destroy` chama `RemoveQueuedEvents(Self)`, que casa pelo `ThreadID` do postador — carimbado pelo `InternalQueue` mesmo com `AThread=nil` (ver `classes.inc`). O post some sem rodar e o objeto de marshal vaza. No Delphi não afeta: o descarte casa só pelo campo `Thread`, que fica `nil`. Morde nos callbacks `OnDisconnect`/`OnReconnect`/`OnReconnectFailed`, que rodam na thread de reconexão da lib (ela termina logo após o `OnReconnect`) — sintoma clássico: o post do `OnDisconnect` chega (a thread ainda vive durante os retries) e o do `OnReconnect` desaparece. Correção: saltar por um worker do `AmqpPool` (threads persistentes) com um `TAMQPWorkItem` que faz o `TThread.Queue` de lá; ver `samples\PublicadorConfiavelVcl\uPublicadorMain.pas` (`TConexaoEventoWork`). Callbacks despachados pelo pool da lib (`OnConfirm`, `OnBasicReturn`, consumers, `OnBlocked`) não têm esse problema.
- `ReportMemoryLeaksOnShutdown` (global do `System`) não existe no FPC → `{$IFNDEF FPC}` em volta da atribuição no `.dpr`.
- LCL não tem a propriedade `Font.Charset` nem `TForm.DesignSize` do jeito que o `.dfm` do Delphi grava — omitir essas linhas ao escrever o `.lfm` equivalente (as demais subpropriedades de `Font` e os `Anchors` de cada controle continuam idênticos nos dois).
- `uses Windows, Messages` nos samples GUI não compila fora do Windows — no FPC, usar a camada de emulação da LCL: `LCLIntf` (`GetScrollInfo`/`SendMessage`, implementadas em todo widgetset, gtk2 incluso), `LCLType` (`TScrollInfo`, `SIF_ALL`, `SB_VERT`, `SB_BOTTOM`) e `LMessages` (`LM_VSCROLL`, mesmo valor `$0115` do `WM_VSCROLL` — não existe unit `Messages` na LCL; alias local `const WM_VSCROLL = LM_VSCROLL` resolve). Vale também no FPC/Windows (widgetset win32), então o condicional é só FPC vs Delphi.
- FPC/Linux sem `cthreads` como primeira unit do `program`: `SyncObjs`/eventos falham em runtime com `ESyncObjectException: Failed to create OS basic event` — as threads da lib (heartbeat, leitura, thread pool) dependem de sincronização de verdade. Vale pra qualquer programa novo que use a lib no Linux, não só ela mesma (aplicado em `SmokeTest.dpr`, `AutorizadorSim.dpr`, `Retaguarda.dpr`, `AutorizadorSimVcl.dpr`, `RetaguardaVcl.dpr` e nos dois runners FPCUnit, sob `{$IFDEF UNIX}`). Nos samples GUI o sintoma aparece só ao clicar Conectar ("Falha ao conectar: Failed to create OS basic event") — a form abre normal.
- Os runners FPCUnit (`AMQPUnitTestsFpc.lpr`/`AMQPIntegrationTestsFpc.lpr`) importavam `Forms`/`Interfaces`/`GuiTestRunner` incondicionalmente pra suportar o modo GUI sem parâmetros — isso força link contra LCL mesmo em modo console. Movido para `{$IFDEF MSWINDOWS}`; fora do Windows os runners rodam sempre em modo console (`consoletestrunner`/`testregistry`), sem precisar de Lazarus/widgetset instalado. `AMQP.TlsIntegrationTests` entra em toda plataforma desde que `EAMQPTls` mudou para `AMQP.Transport` (sempre compilada): sem backend TLS no build, o `Open` do Setup falha e os testes se auto-ignoram, mesmo caminho de broker TLS fora do ar.
- **SIGPIPE no Linux**: `send()` num socket já encerrado mata o processo (exit 141) em vez de devolver erro — visto no SmokeTest `--tls`, porque o destrutor do stream TLS manda `close_notify` best-effort com o socket já derrubado pelo `CloseSocketStream` (no Windows isso é só um erro de send engolido pelo `try/except`). Fix no transporte: `TAMQPTcpSocket.Send` usa `fpsend(..., MSG_NOSIGNAL)` sob FPC+Unix quando a unit `sockets` declara a flag (guarda `Declared(MSG_NOSIGNAL)` no `{$IF}`: cobre Linux e BSDs; Darwin não tem a flag e fica no caminho comum) — EPIPE vira retorno negativo, que o `SendRaw` dos streams TLS converte em `EAMQPTls` engolida. O caminho plain nunca escrevia pós-shutdown, por isso o problema não apareceu na validação Linux original.

## Peças novas em relação à lib original

- `AMQP.Threading`: atomics, `AmqpTickMs`, `TAMQPMonitor` (condvar por gerações de eventos — sem wakeups perdidos; waiters re-checam a condição em loop) e `TAMQPThreadPool` (cresce sob demanda até `max(16, 4×núcleos)`; workers persistentes; `AmqpPool` é o singleton global).
- `AMQP.Transport`: `TAMQPTcpSocket` (Delphi: `System.Net.Socket`; FPC: `ssockets.TInetSocket` + `fpshutdown` para desbloquear a thread de leitura sem corrida de FD).
- Work items em `AMQP.Connection` (implementation): `TAMQPDeliveryWork`, `TAMQPReturnWork`, `TAMQPConfirmWork`, `TAMQPBlockedWork`, `TAMQPUnblockedWork`, `TAMQPReconnectThread` — substituem os closures de `TTask.Run`; o contador in-flight é decrementado no `finally` do `Execute`.
- TLS com dois backends e mesma API (`EAMQPTls` vive em `AMQP.Transport` para existir em toda plataforma):
  - `AMQP.Transport.Tls`: SChannel, compila nos dois compiladores mas **só no Windows** (`AMQP_WINDOWS`, automático).
  - `AMQP.Transport.OpenSSL`: OpenSSL, qualquer plataforma, **opt-in** via `-dAMQP_OPENSSL` (tem precedência sobre SChannel se ambos definidos). Bindings próprios (~26 símbolos com assinatura idêntica em 1.1.1 e 3.x), carregados dinamicamente na 1ª conexão TLS com lista de sonames (`libssl.so.3` → `.1.1`; DLLs `libssl-3-x64` etc. no Windows). O engine SSL trabalha sobre **par de BIOs de memória** (nunca `SSL_set_fd`): preserva o desbloqueio da thread de leitura via close do socket E permite proteger o objeto `SSL` (que não aceita read/write concorrentes) com um `TCriticalSection` interno nunca segurado durante I/O de socket. Sem `AMQP_OPENSSL` fora do Windows, `UseTls` levanta a mesma exceção clara de antes.

## Sub-módulo broker (server) — Fase 1 em andamento (branch `feat/broker`)

Avaliada em 2026-09-02 e iniciada: a "outra ponta" — uma lib para **criar um broker AMQP 0-9-1 dentro de uma app Delphi/FPC**, na mesma codebase (units `AMQP.Server.*`, pacote `pascal_amqp_faa_server.lpk` separado que depende do pacote cliente; quem só usa o cliente não linka nada de server). Plano detalhado e avaliação de modelo de execução: artifact `Broker AMQP — Fase 1` (https://claude.ai/code/artifact/9f07e23c-c200-4435-8770-77164bad0c13).

**Proveniência (reforço da regra inegociável):** o server deriva SÓ da especificação AMQP 0-9-1 e do próprio cliente `pascal-amqp-faa` (MIT, mesmo autor). O servidor RabbitMQ é Erlang/MPL — consulta como conhecimento de protocolo é permitida, transcrição de código não. Nada de outras implementações de broker Pascal/Delphi.

**Escopo Fase 1 ("null broker"):** aceita N conexões, cada uma com N canais; handshake completo; decodifica e aceita todo método cliente→servidor; responde os `*-Ok` corretos; heartbeats bidirecionais; close limpo com o modelo de erro de canal/conexão. **Sem engine**: declares são no-op, publishes descartados, nenhuma entrega real. Fases seguintes: engine em memória (roteamento direct/fanout/topic/headers, consume/get/ack/nack, confirms, mandatory/return), depois durabilidade/TTL/DLX/priority.

**Não-objetivos (todas as fases):** cluster, HTTP management API, federation/shovel, AMQP 1.0, STOMP/MQTT, quorum queues, streams.

**Decisões de arquitetura travadas:**
- Modelo de execução: **thread-por-conexão, I/O bloqueante** (reusa `TAMQPTcpSocket`, `TAMQPMonitor`, `AmqpPool`, o truque `fpshutdown`/`Close` para desbloquear read pendente). Event loop (IOCP/epoll/kqueue) rejeitado — 3 backends, nenhum existe nas units compartilhadas, e o alvo é dezenas–centenas de conexões.
- Escrita: **1 objeto writer por conexão com fila outbound limitada + 1 thread escritora** (backpressure natural = flow control TCP). API `Connection.PostFrames(array of TAMQPFrame)` enfileira method+header+body sob um único lock (senão o cliente vê UNEXPECTED_FRAME por interleave).
- Engine (Fase 2): **ator-por-fila** — cada `TAMQPQueue` tem fila serial de comandos; toda mutação (enqueue, consumer, ack/nack, TTL, delete) passa por ela. Exchanges são lookup quase-imutável sob RWLock. Interface que a Fase 1 já expõe com impl nula: `IAMQPMessageSink.RouteMessage(...)`.
- Mensagem: `TAMQPMessage` com refcount interlocked (`AmqpAtomicInc/Dec`); `Body: TBytes` compartilhado por N filas sem cópia (COW), ninguém muta.
- Lifetime: cada `TAMQPServerConnection` possui sua read-thread/writer/canais; auto-registra/desregistra no broker sob lock; liberação por **reaper explícito** (lista + `OnTerminate`), **nunca `FreeOnTerminate`**.
- Codec: os `Decode` dos métodos cliente→servidor e os `Build` dos métodos servidor→cliente entram **in-place** nas units `AMQP.*.Methods` existentes (adicionam funções, não alteram as existentes) — a suíte cliente (81/81 + 27/27) tem que continuar verde após cada mudança. `TAMQPSocketStream` foi promovido de `AMQP.Connection` (privado) para `AMQP.Transport` (público); `TAMQPTcpListener` vive em `AMQP.Transport`.

**Auth / vhost da Fase 1:** SASL PLAIN só (sem `Secure`/`Secure-Ok`, sem `AMQPLAIN`). `IAMQPAuthenticator` + `TAMQPStaticAuthenticator` (default do broker semeado com `guest`/`guest` — o `TAMQPConnectionParams.Localhost` do cliente conecta sem mudança) + `TAMQPAllowAllAuthenticator` (testes). `TAMQPVirtualHostRegistry` default `{ '/' }`. `IAMQPAuthorizer` existe como stub allow-all para a Fase 2 encaixar. **Desvios deliberados do RabbitMQ (documentados de propósito):** (1) `guest` aceito fora de loopback — o RabbitMQ restringe a conexões locais por padrão (footgun para uso embarcado); (2) sem permissões por vhost; (3) sem delay pós-falha de auth (o param `APeerAddress` de `Authenticate` já está lá para o rate-limit futuro); (4) validação da propriedade `user-id` do `Basic.Publish` só entra com o sink real.

**Modelo por workstream:** WS0/WS1/WS2/WS6/WS7/WS8 (scaffolding, codec, listener, broker skeleton, heartbeat, testes) são mecânicos → Sonnet. WS3 (TLS server), WS4 (handshake FSM), WS5 (regra de interleave de conteúdo) têm correção sutil de protocolo → Opus.

**Progresso:**
- **WS0** concluído (commit `Broker WS0`) — `CLAUDE.md`, `TAMQPSocketStream` promovido para `AMQP.Transport`, `AMQP.Server.Auth` (PLAIN, `TAMQPStaticAuthenticator` default guest/guest), `pascal_amqp_faa_server.lpk`.
- **WS1** concluído (commits `Broker WS1`) — codec bidirecional in-place nas units `AMQP.*.Methods`: `Build` dos métodos servidor→cliente (`BuildStart`/`Tune`/`OpenOk`/`Deliver`/`Return`/`GetOk`/`GetEmpty`/`*DeclareOk`/…) e `Decode` dos cliente→servidor (`DecodeStartOk`/`DecodeOpen`/`DecodeBasicPublish`/`DecodeBasicConsume`/`DecodeQueueDeclare`/…). `DecodeBasicAck/Nack` + `BuildBasicAck/Nack` já eram bidirecionais. Records novos: `TAMQPConnectionStartOk`, `TAMQPConnectionOpen`, `TAMQPQueuePurge`, `TAMQPBasicQosArgs/Publish/Get/CancelArgs/Reject`. SASL response lida como `TBytes` crus (PLAIN tem `#0`). Testes `TConnectionServerCodecTests` (11) + `TServerCodecTests` (25) nas duas suítes, cruzando Build do cliente ↔ Decode do servidor.
- Revalidação do cliente contra broker real: SmokeTest plain e `--tls` PASS (Default e `openssl`), integração 28/28, unitária **117/117** — **nos dois compiladores** (FPC via CLI; Delphi/DUnitX na IDE, 0 leaks).
- **WS2** concluído (commit `Broker WS2`) — `TAMQPTcpListener` em `AMQP.Transport`: FPC com sockets crus (`fpSocket`/`fpBind`/`fpListen`/`fpAccept`/`fpGetSockName`), Delphi com `TSocket.Listen`/`Accept`. `Listen(addr,port,backlog)` (`''`/`0.0.0.0` = todas; port 0 = SO escolhe, `Port` devolve a efetiva); `Accept` bloqueia → `TAMQPTcpSocket` novo ou `nil` se fechado; `Close` de outra thread desbloqueia (Unix: shutdown; Windows: closesocket). `+TAMQPTcpSocket.CreateFromAccepted` — **socket aceito usa fd cru no FPC** (`fprecv`/`fpsend`/`CloseSocket`), NÃO `TInetSocket.Create(handle)`: medido que `shutdown` num socket aceito não desbloqueia o `recv` no Windows (só `closesocket`), e o `TInetSocket` sobre handle não roteava o `Read` por onde o close atua. **Delphi a validar na IDE** (API `System.Net.Socket` escrita sem compilar).
- **WS6 (skeleton) + WS8 (parcial)** concluído (commit `Broker WS6`) — o broker existe como processo:
  - `AMQP.Server.FrameIO`: `TAMQPFrameWriter` — escrita serial por conexão, fila outbound limitada + 1 thread; `PostFrames` atômico (lote method+header+body nunca intercalado); backpressure quando cheia; marca `Failed` e levanta em erro de escrita.
  - `AMQP.Server.Connection`: `TAMQPServerConnection` — thread-por-conexão, lê o protocol-header + drena frames até desconectar (SEM handshake ainda — WS4), ecoa heartbeat; `Shutdown` fecha o socket p/ desbloquear o read; sem `FreeOnTerminate`.
  - `AMQP.Server.Broker`: `TAMQPServer` (Start/Stop idempotente, accept-thread, registro `FActive`/`FDead` + reaper), `TAMQPVirtualHostRegistry` (default `/`; movido para `AMQP.Server.Types` no WS4). Config: `BindAddress`/`Port` (0=efêmera)/`Backlog`/`Authenticator`/`Authorizer`/`VirtualHosts`.
  - Suíte `tests\Server\fpc\AMQPServerTestsFpc.lpi` (FPCUnit) — **14/14**: vhost registry, FrameWriter (FIFO/atomicidade/falha), listener (porta 0/accept+dados/close desbloqueia), skeleton (start-stop, registra+reap, protocol-header inválido, 10 conexões concorrentes, stop com conexões abertas sem travar, heartbeat ecoado). Só FPC por ora (testes de thread/socket; mirror DUnitX depois).
- Regressão: lib default+openssl limpa; unitária 117/117; integração 28/28; SmokeTest plain+`--tls` PASS.
- **WS4** concluído (commit `Broker WS4`) — **o cliente real da lib conecta no broker**:
  - **Units do server mudaram-se para `src\server\`** (mesmos nomes de unit). Motivo: com tudo em `src\`, o `OtherUnitFiles=..\src` do pacote server fazia o FPC recompilar units do cliente para dentro de `lib\srv-*`, e qualquer projeto que usasse os dois pacotes pegava duas cópias de `AMQP.Wire.ppu` → `checksum changed` / `Can't find unit`. Com o diretório separado, o pacote server só compila as suas e consome o `.ppu` do pacote cliente.
  - `AMQP.Server.Types` (novo): `TAMQPVirtualHostRegistry` (veio do Broker — a FSM precisa consultá-lo e não pode usar a unit que a usa), `TAMQPServerConnConfig` (auth/authorizer/vhosts + limites do Tune, copiada para cada conexão aceita) e as exceções do modelo de erro: `EAMQPChannelError` (Channel.Close, conexão sobrevive), `EAMQPConnectionError` (Connection.Close + espera Close-Ok), `EAMQPServerAbort` (derruba o socket sem Close). **Quem mexe em vhosts agora precisa de `AMQP.Server.Types` no uses.**
  - `AMQP.Server.Channel` (novo): `TAMQPServerChannel` — id, estado (`amqchOpen`/`amqchClosing`) e flag do Channel.Flow. WS5 pendura aqui a montagem de conteúdo.
  - `AMQP.Server.Connection`: FSM completa (`amqssHeader` → `AwaitStartOk` → `AwaitTuneOk` → `AwaitOpen` → `Open` → `Closing` → `Closed`), SASL PLAIN pelo `IAMQPAuthenticator`, validação do Tune-Ok (não pode exceder o proposto nem cair abaixo dos 4096 da spec), vhost pelo registry, mapa de canais com Channel.Open/Close/Flow, e dois hooks virtuais para o WS5 (`DispatchChannelMethod` / `DispatchContent`) que por ora recusam → canal fecha com 540.
  - `AMQP.Protocol`: constantes de reply-code (`AMQP_REPLY_SUCCESS`, `AMQP_ACCESS_REFUSED`, `AMQP_FRAME_ERROR`, `AMQP_COMMAND_INVALID`, `AMQP_CHANNEL_ERROR`, `AMQP_UNEXPECTED_FRAME`, `AMQP_NOT_ALLOWED`, `AMQP_NOT_IMPLEMENTED`, …), com a classificação canal/conexão anotada.
  - `AMQP.Frame`: `EAMQPFrameEOF` (subclasse de `EAMQPFrame`) para o fim de stream, separando "peer desconectou" (encerramento silencioso) de frame malformado (501 + Connection.Close). Os dois testes de stream truncado passaram a esperar a subclasse.
  - `TAMQPServer` ganhou `ChannelMax`/`FrameMax`/`Heartbeat` (defaults iguais aos do RabbitMQ: 2047/131072/60, que casam com o `TAMQPConnectionParams.Localhost`) e `Connections` (snapshot para log/testes).
  - Suíte `tests\Server\fpc` — **32/32**: as 14 do esqueleto + `TClientHandshakeTests` (8, com o `TAMQPConnection` real: conecta, senha errada → 403, vhost inexistente → 530, vhost extra, canais abrem/fecham, método não implementado fecha só o canal, 2 conexões simultâneas, close limpo) + `TRawHandshakeTests` (10, socket cru: Start anuncia PLAIN/product, método fora de ordem → 503, Connection.* fora do canal 0 → 503, Tune-Ok acima do proposto e frame-max < 4096 → 530, segundo Channel.Open e canal não aberto → 504, tipo de frame desconhecido → 501, conteúdo sem publish → 505, Close → Close-Ok).
  - **Dois bugs achados por esses testes:**
    1. *No broker*: a conexão respondia o `Connection.Close-Ok` mas só fechava o socket quando o reaper liberava o objeto. Um cliente que fecha limpo ficava pendurado no `recv` (no Windows o `fpshutdown` do socket cliente não desbloqueia um recv pendente — o RabbitMQ não expõe isso porque fecha o TCP logo após o Close-Ok). Corrigido no `finally` do read loop: `FWriter.Stop` (drena o Close-Ok) e **então** `FSocket.Close`.
    2. *No cliente* (`AMQP.Connection`): `TAMQPChannel.Destroy` só desregistrava do `FChannels` da conexão via `Close`, que faz early-exit quando o canal já está fechado — exatamente o caso de o **servidor** ter mandado `Channel.Close` por erro de canal. O dicionário ficava com ponteiro pendurado e o destrutor da conexão dava AV. Agora o destrutor sempre chama `UnregisterChannel`. Vale para qualquer broker, RabbitMQ incluso.
- Regressão pós-WS4/WS8 **nos dois compiladores**: FPC — unitária 117/117, integração 28/28 (RabbitMQ real), server 32/32, SmokeTest plain e `--tls` PASS, lib+server compilando limpo com e sem `-dAMQP_OPENSSL`. Delphi (IDE) — unitária 117/117, integração 28/28 e **server 32/32**, todas com **0 leaks**, SmokeTest `--tls` PASS (SChannel), Build All do `AMQP.groupproj` limpo. Com o WS8 o `src\server\` passou a ser compilado e exercitado pelos dois compiladores.

- **WS8 (mirror DUnitX do server)** concluído — `tests\Server\AMQP.ServerTests.dproj` espelha as 32 asserções da suíte FPCUnit e entrou no `AMQP.groupproj` (targets `AMQP_ServerTests`): **32/32, 0 leaks**. Foi a primeira vez que `src\server\` passou por um compilador Delphi — WS2/WS4/WS6 só tinham sido compilados pelo FPC — e a execução rendeu de imediato três defeitos no ramo Delphi do `System.Net.Socket`, todos invisíveis ao FPC.
  - Os três defeitos, todos no ramo Delphi (`System.Net.Socket`) e todos no mesmo tema — **a RTL levanta no `shutdown` ANTES de fechar o handle**:
    1. Socket **aceito** fecha com `FSock.Close(True)`. O `Close` padrão da RTL (`ForceClosed=False`) faz `shutdown(SD_BOTH)` e então **drena o socket num `while recv(...) > 0` na thread que chamou** — roda `recv` concorrendo com a thread de leitura da conexão, e no Windows o `shutdown` num socket aceito não desbloqueia um `recv` pendente (o mesmo achado do WS2 que levou o caminho FPC a usar fd cru + `CloseSocket`). `Close(True)` pula shutdown/drain e vai direto ao `closesocket`. O caminho **cliente** segue com o `Close` padrão (shutdown gracioso), validado contra brokers reais desde a lib original.
    2. **`TAMQPTcpListener.Close` também usa `Close(True)`** e o `Destroy` guarda o `FListen.Free`. Um socket de ESCUTA tem `FState = [Connected, Listening]` (a RTL marca `Connected` no bind), mas nunca esteve conectado de fato — então o `shutdown(SD_BOTH)` do `Close` padrão falha com WSAENOTCONN e levanta antes do `closesocket`. Resultado: `Accept` pendente **nunca desbloqueava** (o `Close_DesbloqueiaAccept` travava) e o `Free` do listener levantava. É a mesma regra que o caminho FPC/Windows já documentava: num listen socket quem desbloqueia o accept é o `closesocket`, não o `shutdown`.
    3. **`Close` nunca escapa** (e `Destroy` guarda o `FSock.Free`). O `shutdown` do `Close` padrão levanta `ESocketError` WSAENOTCONN (10057) quando o peer JÁ derrubou o TCP — o que passou a acontecer sempre porque o broker fecha o socket logo após o `Connection.Close-Ok`. Pior: a RTL levanta **antes** de limpar o `FState`, então o `TSocket.Destroy` refaz o `shutdown` e a exceção sai de dentro do `Free`. Agora o `except` força `Close(True)` para tirar o socket de "Connected". Sem isso, todo teste do cliente erra no teardown com "on API 'shutdown'" — e há risco de o `StopReadThread` travar no `WaitFor`, já que a exceção abortava o `Close` antes do `closesocket` que desbloqueia o `recv` da thread de leitura. **Vale para qualquer peer que feche o TCP abruptamente, não só o broker desta lib.**

- **WS7 (heartbeat)** concluído — o broker emite heartbeat pelo próprio timer, detecta peer morto e faz valer o prazo do `Connection.Close-Ok`:
  - **Uma thread monitora para o broker inteiro** (`TAMQPMonitorThread`), não um timer por conexão: a cada `AMQP_SERVER_MONITOR_TICK_MS` (200 ms) tira um snapshot das conexões vivas e chama `HeartbeatTick` + `EnforceCloseDeadline` em cada uma. Escala para centenas de conexões sem multiplicar threads, e como a thread da conexão está bloqueada no read, o timer tinha mesmo de ser externo.
  - `TAMQPServerConnection.HeartbeatTick` (spec 4.2.7), sobre o intervalo do Tune-Ok: envio ocioso há ≥ metade do intervalo → posta um heartbeat; nada recebido há > 2× o intervalo → peer morto, fecha o socket (o que desbloqueia o read e encerra pelo caminho normal). No-op antes do Tune-Ok e com intervalo 0.
  - **O eco de heartbeat do WS6 saiu.** Heartbeat não se responde — cada lado emite pelo próprio timer; receber já vale como sinal de vida (o `FLastReadTick` é atualizado no loop de leitura, para qualquer frame, não só heartbeat).
  - Ociosidade de envio é medida pelo **flush real no socket** (`TAMQPFrameWriter.LastWriteTick`), não pelo `Post*`: um frame parado na fila ainda não saiu no wire.
  - **O reap mudou de dono**: era feito no `AcceptLoop` (uma conexão morta só era liberada na *próxima* conexão aceita); agora é a monitora que recolhe, a cada tick. O `Stop` derruba a monitora **antes** de tocar nas conexões — dois reapers concorrentes seriam double-free.
  - `TAMQPServer.CloseTimeoutMs` (default `AMQP_SERVER_CLOSE_TIMEOUT_MS`) tornou o prazo do Close-Ok configurável — sem isso os 10 s fixos não eram testáveis.
  - Suítes do server passam a **37/37** nos dois compiladores: `THeartbeatTests` (5) cobre broker emitindo sozinho, peer mudo derrubado em 2× o intervalo, heartbeat 0 não derruba, Close-Ok não respondido derrubado no prazo, e o **cliente real ocioso 3 s com heartbeat de 1 s dos dois lados sem ninguém derrubar ninguém**. O antigo `HeartbeatEcoado` virou `HeartbeatRecebido_NaoDerrubaNemResponde` (o eco não existe mais).
  - **Quarto defeito Delphi no `System.Net.Socket`, achado pelos testes novos** (os três primeiros estão no WS8): `TAMQPTcpSocket.Receive` violava o contrato da própria unit — "Receive devolve <= 0 em fim de stream/erro" — porque o `TSocket.Receive` do Delphi levanta `ESocketError` em vez de devolver. Aparece como **WSAECONNRESET (10054)** quando o peer fecha com RST em vez de FIN, exatamente o que o broker faz ao derrubar por heartbeat ou por prazo do Close-Ok. O FPC nunca expôs isso: `fprecv` devolve -1. Agora o ramo Delphi captura e devolve -1, e os dois compiladores concordam. Os backends TLS já tratavam `<= 0` (o de OpenSSL até documentava o quirk), então nada muda para eles. **Vale para o cliente contra qualquer broker que dê RST**, RabbitMQ incluso.
- Regressão pós-WS7 **nos dois compiladores**: FPC — server 37/37, unitária 117/117, integração 28/28, SmokeTest plain e `--tls` PASS, server compilando limpo com e sem `-dAMQP_OPENSSL`. Delphi (IDE), **nos build configs Debug E OpenSSL** (por causa da mudança no `Receive`, que é caminho do cliente): server 37/37, unitária 117/117 e integração 28/28, todas com **0 leaks**; SmokeTest plain e `--tls` PASS nos dois backends TLS (SChannel no Debug, OpenSSL 3.5.2 no config OpenSSL).

- **WS5 (conteúdo + interleave + despacho nulo)** concluído — com ele a Fase 1 fecha o escopo de protocolo: o broker **decodifica e responde todo método cliente→servidor**.
  - `AMQP.Server.Channel` ganhou a montagem de conteúdo: `BeginContent` (Basic.Publish) → `SetContentHeader` → `AppendBody` até fechar o body-size. Vive **no canal**, não na conexão, porque é exatamente essa a granularidade da regra da spec.
  - **Regra de interleave (spec 2.3.5)**: entre o Basic.Publish e o último body frame, nenhum outro frame pode aparecer *naquele canal* — nem outro método, nem Channel.Close → `UNEXPECTED_FRAME` (505), erro de conexão. Canais diferentes seguem livres para intercalar, e há teste com dois publishes entrelaçados provando isso.
  - Outras violações que viram 505: content-header sem Publish antes, body sem header, corpo maior que o body-size declarado, e content-header cuja class-id não é a da classe do método (lida direto do payload, já que o `DecodeContentHeader` consome e descarta esse campo).
  - O corpo cresce conforme chega — nada de alocar o body-size declarado de uma vez, senão um peer hostil derruba o broker só anunciando um corpo gigante.
  - `IAMQPMessageSink` + `TAMQPNullMessageSink` (em `AMQP.Server.Types`), plugável por `TAMQPServer.MessageSink`: é o ponto onde a engine da Fase 2 entra sem mudar assinatura de chamador nenhum. A mensagem e a tabela de headers pertencem ao **canal** e somem quando `RouteMessage` retorna — quem quiser guardar, copia.
  - Despacho nulo de Exchange/Queue/Basic/Confirm: cada método é decodificado e respondido com o `*-Ok` correto, **respeitando o `no-wait`** (que suprime a resposta) e gerando nome quando o cliente manda vazio (`amq.gen-` para fila anônima, `amq.ctag-` para consumer-tag). `Basic.Get` devolve sempre `Get-Empty`; ack/nack/reject não respondem nada. `Basic.Qos` e `Confirm.Select` guardam o estado no canal para a Fase 2 usar.
  - **Tx (classe 90) fica de fora de propósito** — não está no codec e transações foram descartadas neste projeto (ver decisão no roadmap); cai no `False` do dispatch e o canal morre com 540, que é o teste `ClasseNaoImplementada_FechaSoOCanal`.
  - **Limitação conhecida da Fase 1:** `Confirm.Select` é aceito mas o broker nunca manda `Basic.Ack` — um cliente que chame `WaitForConfirms` contra este broker fica esperando até o timeout. Confirms de verdade são Fase 2, junto com a engine.
  - Suítes do server passam a **47/47** nos dois compiladores. A fixture nova `TContentTests` (10) usa um `TRecordingSink` que copia o que recebeu, então os testes conferem a remontagem de verdade: corpo de 300 KB partido em vários body frames volta byte a byte, content-type/correlation-id/headers chegam intactos, mensagem sem corpo (body-size 0) é aceita. `MetodoNaoImplementado_FechaSoOCanal` saiu do fixture do cliente — `Queue.Declare` agora funciona — e virou o teste de Tx acima.
- Regressão pós-WS5 **nos dois compiladores**: FPC — server 47/47, unitária 117/117, integração 28/28, SmokeTest plain e `--tls` PASS, server limpo com e sem `-dAMQP_OPENSSL`. Delphi (IDE) — server 47/47 com **0 leaks** nos build configs Debug e OpenSSL. (O WS5 não tocou no cliente: só `src\server\`, os testes e este arquivo.)
  - Duas divergências que só o espelho DUnitX pegou: o helper `ExpectChannelClose` existia apenas na suíte FPCUnit, e um teste vazava a `TAMQPFieldTable` de headers — `SetHeaders` **não copia** e o `Publish` **não libera**, o dono é o chamador (mesma convenção documentada no sample `EventosHeadersVcl`). O FPCUnit não reporta leaks; o DUnitX sim. É um bom motivo para o espelho existir mesmo quando a lógica é idêntica.

- **WS3 (TLS server-side)** concluído — o broker aceita conexões cifradas, e com isso **a Fase 1 está completa**.
  - `TAMQPOpenSslStream.CreateServer(AUnderlying, ACertFile, AKeyFile)`: um segundo construtor na MESMA classe do cliente, não uma classe nova. Só o setup difere (`TLS_server_method`, cert+chave em PEM, `SSL_set_accept_state`); o par de BIOs de memória, os dois locks, a drenagem, `Read`/`Write` e o `close_notify` são exatamente os mesmos — e já validados por toda a suíte TLS do cliente. Bindings novos: `TLS_server_method`, `SSL_CTX_use_certificate_chain_file`, `SSL_CTX_use_PrivateKey_file`, `SSL_CTX_check_private_key`, `SSL_set_accept_state`.
  - `SSL_CTX_check_private_key` no setup: cert e chave de pares diferentes é o erro de configuração clássico, e é melhor falhar ao aceitar a conexão que num handshake obscuro depois.
  - **O handshake roda na thread da CONEXÃO, não na de accept.** Por isso o `FStream` e o `FWriter` deixaram de nascer no construtor e passaram para `SetupTransport`, chamado no início do `RunReadLoop`: um handshake TLS é bloqueante e um cliente lento (ou hostil) travaria o accept do broker inteiro. A conexão passou a guardar `FRawStream` (socket) e `FStream` (o mesmo objeto em plain; o wrapper TLS quando cifrado, e aí o wrapper é dono do raw).
  - `TAMQPServer.UseTls` / `TlsCertFile` / `TlsKeyFile`. É porta dedicada tipo 5671 (AMQPS), não STARTTLS. A resposta ao protocol-header inválido passou a sair pelo `FStream` em vez de direto no socket, senão iria em claro numa conexão TLS.
  - **Só com `-dAMQP_OPENSSL`.** O backend SChannel desta lib implementa apenas o lado cliente; sem OpenSSL, `UseTls=True` levanta `EAMQPTls` com a mensagem explicando. **SChannel server-side fica como trabalho futuro** — exige `AcquireCredentialsHandle` com cert do store/PFX e um laço de `AcceptSecurityContext`, que é uma superfície bem maior que a do cliente.
  - **Bug achado pelos testes:** quando o construtor do wrapper TLS falha (cert inexistente, cliente em claro na porta TLS), o destrutor auto-chamado já libera o stream de baixo — e a conexão liberava de novo. Era **double-free em toda falha de handshake**. O `SetupTransport` agora solta a referência (`FRawStream := nil`) antes de repropagar.
  - Testes: `TServerTlsTests` (4), compilada só sob `-dAMQP_OPENSSL` — cliente real conecta e abre canal sobre TLS, publica 300 KB (vários body frames E vários registros TLS) com o corpo conferido byte a byte, cliente em claro na porta TLS não abre, e certificado inexistente derruba a conexão. Usa os certs self-signed de `docker\certs`, achados subindo a partir da pasta do executável (as duas suítes rodam de profundidades diferentes).
  - **A suíte do server tem contagens diferentes por build**: 47 no Default e **51 no `openssl`**. É honesto — no build Default o broker realmente não tem TLS. O `AMQPServerTestsFpc.lpi` ganhou o build mode `openssl` (mesmo padrão dos outros runners: define numa `SharedMatrixOptions` amarrada ao mode).
- Regressão pós-WS3 **nos dois compiladores**: FPC — server 47/47 (Default) e 51/51 (`openssl`), unitária 117/117, integração 28/28, SmokeTest plain e `--tls` PASS. Delphi (IDE) — server **47/47 no Debug e 51/51 no OpenSSL**, ambos com 0 leaks. O run OpenSSL exercita os dois lados do backend de uma vez: o `TAMQPConnection` real (construtor cliente) conectando no broker (construtor servidor), com todos os símbolos novos carregados pelo `EnsureOpenSsl` compartilhado.

**Fase 1 completa.** O broker faz handshake com SASL PLAIN, canais, todo método cliente→servidor com o `*-Ok` correto, remontagem de conteúdo com a regra de interleave, heartbeats bidirecionais, modelo de erro canal-vs-conexão e TLS. Falta engine: nada é roteado, entregue ou confirmado.

**Próximo: Fase 2** (engine de roteamento em memória), que entra pelo `IAMQPMessageSink`: exchanges direct/fanout/topic/headers, filas com ator-por-fila, consume/get/ack/nack, publisher confirms (hoje o `Confirm.Select` é aceito mas nenhum `Basic.Ack` é enviado) e mandatory/Basic.Return.

**Build/teste do server:** `fpc -Fusrc -Fusrc\server -Fisrc -FEbuild -FUbuild src\server\AMQP.Server.Broker.pas` compila o server inteiro; pacote `lazbuild packages\pascal_amqp_faa_server.lpk`. Suíte em `tests\Server\AMQP.ServerTests.dproj` (DUnitX, dentro do `AMQP.groupproj`) e `tests\Server\fpc\AMQPServerTestsFpc.lpi` (FPCUnit) — **espelhos um do outro: mesmos nomes de teste e mesmas asserções, mantenha os dois em sincronia**. A fixture sobe o broker in-process em porta efêmera e aponta o `TAMQPConnection` do cliente nele. Certs de TLS: reusar `docker\certs`.

## Build e testes

- FPC: `fpc -Fusrc -Fisrc -FEbuild -FUbuild src\AMQP.Connection.pas` compila a lib inteira (somar `-dAMQP_OPENSSL` para o backend OpenSSL). Pacote Lazarus: `lazbuild packages\pascal_amqp_faa.lpk`.
- Lazarus/lazbuild com OpenSSL: `SmokeTest.lpi` e os dois runners FPCUnit têm um **build mode `openssl`** (`lazbuild -B --build-mode=openssl <proj>.lpi` ou dropdown na IDE). O define vai numa `SharedMatrixOptions` amarrada ao mode — é o único jeito de alcançar as units do **pacote** (Custom Options do projeto não recompilam o pacote; foi exatamente esse o erro ao tentar via IDE). O `Default` segue SChannel/plain. Trocar de mode recompila o pacote inteiro (cache de `.ppu` único). No runner unitário o mode existe só por consistência: os testes unitários não linkam `AMQP.Transport`, então o exe não muda.
- Smoke test de integração: `samples\SmokeTest\SmokeTest.dpr` (mesmo fonte para FPC e Delphi) contra o RabbitMQ do `docker/docker-compose.yml` — cobre handshake, topologia, confirms, get, consume+ack e reconexão com recovery. Com o argumento `--tls`, roda tudo sobre TLS (5671 do `docker-compose.tls.yml`) — SChannel no Windows, OpenSSL se compilado com `-dAMQP_OPENSSL`. **Rode-o após qualquer mudança na lib.**
- Delphi da máquina é Community Edition: **não compila por linha de comando** ("This version of the product does not support command line compiling") — validar o lado Delphi abrindo `samples\SmokeTest\SmokeTest.dproj` no IDE.
- Sincronização pós-reconexão em testes: esperar `OnReconnect` (dispara após o recovery), não `IsOpen` (fica True antes do replay da topologia).

## Convenções gerais

Mesmo padrão dos projetos irmãos (`delphi-amqp-faa`, `delphi-api-infra-faa`): licença MIT com copyright de Fabiano Arndt, commits em português, sem pushes/commits automáticos sem confirmação explícita do usuário.

O README tem duas versões: `README.md` (português, **canônico**) e `README.en.md` (inglês, com identificadores/comentários dos exemplos traduzidos). **Toda mudança no README.md deve ser replicada no README.en.md** na mesma sessão.

## Roadmap

1. ~~Porte do núcleo + validação FPC/Win64 com smoke test (broker real).~~ **Concluído.**
2. ~~Validação Delphi via IDE (CE não tem CLI).~~ **Concluído** — `SmokeTest.exe` (Win32\Debug) rodou os 7 passos com PASS.
3. ~~TLS multiplataforma via OpenSSL.~~ **Concluído** — `AMQP.Transport.OpenSSL.pas` (ver "Peças novas"), opt-in via `-dAMQP_OPENSSL`, validado no Linux x86_64 (container Debian, libssl 3): SmokeTest `--tls` PASS nos 7 passos (reconexão sobre TLS inclusa) e suíte FPCUnit de integração 27/27 com os 5 testes TLS conectando de verdade (inclusive a rejeição de cert self-signed com `TlsVerifyPeer=True`, que levanta `EAMQPTls` como no SChannel, payload de 300KB, consumo concorrente e handshake contra porta plain). No Windows, tudo revalidado: build padrão (SChannel) intacto — SmokeTest plain e `--tls` PASS, 80/80 + 27/27 — e o build com `-dAMQP_OPENSSL` validado também no **Delphi** (suíte de integração 27/27 na IDE, Win32, TLS real sobre OpenSSL 3.5.2 32-bit — as DLLs vieram do `C:\Windows\SysWOW64`, sem precisar copiar pro exe; conferir com `TLS_client_method` no binário + tempos reais nos testes TLS de que o backend ativo é mesmo o OpenSSL).
   - **Pendente**: validar o backend OpenSSL compilado pelo **Delphi em Linux** — a Community Edition não tem o target. O único trecho Delphi-específico é o `LoadLibrary`/`GetProcAddress` POSIX (que no Delphi vem do `SysUtils`); o restante é o mesmo fonte validado pelo FPC/Linux.
4. ~~Porte dos testes unitários e de integração da lib original (Delphi/DUnitX e FPC/FPCUnit).~~ **Concluído.**
   - Fase Delphi/DUnitX **concluída** — `tests\Unit\AMQP.UnitTests.dproj` (80/80, 0 leaks) e `tests\Integration\AMQP.IntegrationTests.dproj` (27/27, 0 leaks, contra o RabbitMQ do `docker/docker-compose.yml`; rodado na IDE nos dois backends TLS — SChannel no build padrão e OpenSSL com `-dAMQP_OPENSSL`). Nos testes de integração os callbacks anônimos (`OnBasicReturn`/`OnConfirm`/`OnReconnect`/`Consume`) viraram métodos nomeados na fixture, já que a lib usa `procedure ... of object` (regra do FPC). `AMQP.groupproj` na raiz abre os dois projetos juntos no IDE.
   - Fase FPC/FPCUnit dos **unitários concluída** — `tests\Unit\fpc\AMQPUnitTestsFpc.lpi` (mesma cobertura do `AMQP.UnitTests.dproj`, reescrita para a API do FPCUnit — `AssertEquals`/`AssertTrue` com mensagem primeiro, `AssertException(Classe, MetodoDeObjeto)` no lugar de `Assert.WillRaise`), 80/80, validado por `fpc` e `lazbuild` direto (sem precisar do IDE). Ver gotchas de FPCUnit/`TValue` acima. O runner decide sozinho pelo `ParamCount`: sem argumentos abre a GUI do FPCUnit (pacote `FPCUnitTestRunner`, árvore de testes + barra verde/vermelha); com argumentos (`--all --format=plain`) roda em modo console.
   - Fase FPC/FPCUnit de **integração concluída** — `tests\Integration\fpc\AMQPIntegrationTestsFpc.lpi`, 27/27 (0 erros, 0 falhas), rodado contra o RabbitMQ real do `docker/docker-compose.yml`, incluindo TLS de verdade (`docker-compose.tls.yml` + certs de `docker/certs`) — confirmado com o broker recriado com a porta 5671 ativa; os 5 testes de TLS (publish/busca, verify-peer, payload 300KB, consumo concorrente, porta plain) conectaram e passaram de fato, não caíram no caminho de auto-ignorar por broker indisponível. A asserção de concorrência (pico > 1) dos testes de consumo é determinística: o callback espera OBSERVAR outro rodando junto (timeout 2s, curto-circuito com pico já registrado) em vez de um sleep fixo, que flakeava sob carga. Precisou de `AmqpAtomicSet`/`AmqpAtomicCompareExchange` novos em `AMQP.Threading` (substituem `TInterlocked`, que não existe no FPC) e de um `TAMQPWorkItem` customizado via `AmqpPool` no lugar de `TParallel.For` (`System.Threading` também não existe no FPC). Ver gotchas acima.
5. Validação em Linux — **concluída** (x86_64, container Debian/Docker, FPC 3.2.2, contra o RabbitMQ real do `docker/docker-compose.yml`, compartilhando o network namespace do container do broker pra `localhost` resolver certo): lib inteira compila limpo; `SmokeTest` PASS nos 7 passos (com reconexão); suítes FPCUnit 80/80 unitários e 27/27 integração (TLS incluso via OpenSSL com `-dAMQP_OPENSSL` — item 3 concluído; sem a diretiva os 5 de TLS se auto-ignoram e a suíte segue 27/27). Ver gotchas de `cthreads`, do runner FPCUnit console-only e de SIGPIPE acima. `Retaguarda` conectava via `TAMQPConnectionParams.LocalhostTls` por padrão (só funcionava no Windows) — trocado para `Localhost` puro, igual `AutorizadorSim`, pra dar pra testar o par no Linux também; ambos precisaram do mesmo `cthreads`. Testado o par completo (publica + consome concorrente) contra o broker real. **ARM64 validado** (container Debian bookworm `--platform linux/arm64` via QEMU do Docker Desktop, FPC 3.2.2/aarch64 do apt, mesma receita de network namespace): lib compila nos dois modos, SmokeTest PASS plain e `--tls` (OpenSSL 3.0.20 aarch64, `libssl.so.3`), suítes 80/80 e 27/27 com os 5 testes TLS conectando de fato (I:0 no relatório). **Samples VCL/LCL validados** (x86_64, LCL/GTK2: `apt install lazarus lcl-gtk2` + `lazbuild --ws=gtk2`, rodados sob Xvfb com cliques via xdotool): o par completo funcionou contra o broker real — Autorizador publicou 6 notas e a Retaguarda consumiu com workers concorrentes do pool. Precisou dos ajustes de `uses` LCLIntf e `cthreads` (gotchas acima). Item concluído; a única pendência de plataforma restante é o Delphi/Linux do item 3 (bloqueado pela CE).
6. Samples de padrões de mensageria (sequência decidida em 2026-07-14; todos GUI dual VCL/LCL, mesmo padrão dos samples existentes; transações AMQP foram avaliadas e descartadas — confirms cobrem o caso de uso):
   1. ~~Publicador confiável — publisher confirms de ponta a ponta (ConfirmSelect, OnConfirm, Basic.Return de mandatory sem rota, WaitForConfirms, RepublishUnconfirmedOnReconnect com queda de broker no meio do lote).~~ **Concluído** — `samples/PublicadorConfiavelVcl`; rendeu o gotcha do `TThread.Queue` descartado (lista acima).
   2. ~~RPC request/reply — `ReplyTo` + `CorrelationId` + `Expiration`, fila de callback exclusiva/auto-delete com nome fixo por instância (nome gerado pelo broker não sobrevive ao replay do recovery), timeout no cliente e descarte de resposta tardia.~~ **Concluído** — `samples/ConsultaStatusVcl`, validado nos dois lados contra o broker real (timeout sem servidor com o pedido expirando na fila via `Expiration`, 5 consultas concorrentes, resposta tardia descartada pela correlação; FPC dirigido via UIA, Delphi na IDE). **Ampliado em 2026-07-15** com um toggle "Usar Direct Reply-to": alterna o cliente entre a fila de resposta nomeada e o pseudo-endereço `amq.rabbitmq.reply-to` (consumir em no-ack no MESMO canal do publish, sem declarar fila — restrições confirmadas na doc do RabbitMQ), fechando a inconsistência de o README recomendar Direct Reply-to para RPC sem nenhum sample mostrá-lo. Lado servidor idêntico nos dois modos. Round-trip validado contra o broker (o servidor recebe o ReplyTo reescrito pelo broker) e exercitado na GUI nos dois modos com a troca ao vivo.
   3. ~~Dead-letter + retry com backoff — `x-dead-letter-exchange` + fila de espera com `x-message-ttl`, DLQ após N tentativas; exercita os `Arguments` (`TAMQPFieldTable`) do declare.~~ **Concluído** — `samples/RetryDlqVcl`, validado nos dois lados contra o broker real (sucesso na 3ª tentativa após 2 backoffs e DLQ após esgotar; tentativas contadas pelo `x-death`). Rendeu duas mudanças na lib: `TAMQPChannel.DeleteQueue` (com reconciliação de recovery por decode dos payloads gravados) e o fix do `AmqpUnwrapValue` (gotcha acima). Revalidado tudo: SmokeTest plain/TLS PASS, FPCUnit 81/81, DUnitX 81/81 com 0 leaks (o teste novo `Decode_ArrayDeTabelas_EstiloXDeath` cobre o unwrap e o destrutor nos dois compiladores).
   4. ~~Pub/sub com exchange topic — binding keys distintas (`nota.aprovada`, `nota.#`) em consumidores separados.~~ **Concluído** — `samples/EventosTopicVcl`, validado nos dois lados contra o broker real (fan-out do mesmo evento a 2 assinantes, roteamento seletivo por `*`/`#`, evento sem assinante devolvido via mandatory+Basic.Return, remoção de assinante com Cancel→Unbind→DeleteQueue reconciliando o recovery — testada no Delphi, onde a seleção de ListView é acessível); único sample que exercita `DeclareExchange`/`BindQueue`/`UnbindQueue`. **Item 6 completo: os 4 samples de padrões entregues.**
7. Samples adicionais avaliados (2026-07-15; oportunidades de padrões ainda não cobertos; mesma forma GUI dual VCL/LCL dos existentes):
   1. ~~Headers exchange (`AMQP_EXCHANGE_TYPE_HEADERS`) — roteamento por casamento de campos de header (`x-match: all`/`any`) em vez de routing key.~~ **Concluído em 2026-07-16** — `samples/EventosHeadersVcl`, molde do `EventosTopicVcl`. Único sample que exercita *binding arguments* (`TAMQPFieldTable` no `BindQueue`) e a propriedade `Headers` da mensagem (`SetHeaders` no publish, leitura via `LerHeader` na entrega — `TryGetValue` para `TValue` local + `.AsString`, padrão seguro do FPC). O `UnbindQueue` repete os mesmos argumentos do bind (é o argumento que identifica o binding no headers exchange); os critérios ficam em campos simples do `TAssinante` para reconstruir a tabela idêntica. Roteamento validado contra o broker por smoke test console espelhando a lib (4 cenários: `all`/`any` casando certo e `mandatory`+`Basic.Return` quando nada casa); GUI compila limpo no FPC/LCL (`lazbuild`). **Validado também no Delphi (IDE, CE)**: conectou, declarou o exchange, criou assinantes `all`/`any` com binding arguments e publicou com headers — com 2 assinantes (`all` nfe+sul e `any` nfe/sul) e 4 publishes, os contadores bateram (Sub `all`=1, Sub `any`=4) e nenhum `Basic.Return` espúrio. Está no `AMQP.groupproj`. FPC gotcha do `TValue.From<T>` inline em `.Put` encadeado evitado com Puts separados, como no `RetryDlqVcl`.
   2. ~~Fila com prioridade (`x-max-priority` no declare + propriedade `Priority`/`SetPriority`) — mensagem de prioridade maior fura a fila.~~ **Concluído em 2026-07-16** — `samples/PrioridadeVcl`. Único sample que exercita a propriedade `Priority` (`SetPriority` no publish, leitura na entrega). Roteiro na tela: com o consumidor parado, publica um lote de prioridades misturadas (backlog) e só então inicia o consumidor lento — a saída vem em ordem decrescente de prioridade. Duas condições essenciais embutidas: `Qos(prefetch)` baixo (com prefetch alto o broker despeja um lote no cliente e a prioridade não reordena o que já saiu da fila — o campo Prefetch é editável de propósito pra ver isso quebrar) e consumidor mais lento que o produtor (delay + ack manual, `Sleep` ANTES do `Ack`). Conectar recria a fila via `DeleteQueue` (redeclarar com `x-max-priority` diferente é `PRECONDITION_FAILED`). Validado contra o broker por smoke test console espelhando a lib (lote `2 8 5 9 1 7 3 9 0 6` drenado como `9 9 8 7 6 5 3 2 1 0`); GUI compila limpo no FPC/LCL (`lazbuild`). **Validado também no Delphi (IDE, CE)**: lote + uma mensagem avulsa p9 publicada depois (consumidor parado) drenaram como `9 9 9 8 7 6 5 3 2 1 0` — monotônico decrescente, e os três p9 saíram em ordem de publicação (`pub#4`→`pub#8`→avulsa), confirmando FIFO dentro do mesmo nível de prioridade. Nos dois group-projects (`AMQP.groupproj` e `AMQP.lpg` — este último ganhou também o `EventosHeadersVcl`, que faltava). **Item 7 completo: os 2 samples avaliados entregues.**

   Avaliados e descartados na mesma rodada: exchange fanout/direct (subconjunto do topic — o `EventosTopicVcl` já mostra broadcast via binding key `#`); alternate exchange (delta pequeno sobre o Basic.Return já coberto nos samples de pub/sub e confirms); blocked/unblocked (a lib tem `OnBlocked`/`OnUnblocked` sem sample, mas demonstrar exige disparar alarme de memória/disco do broker — material ruim pra sample). A suíte já cobre bem os padrões comuns; daqui pra frente o risco é repetir mecânica com roupagem diferente.
